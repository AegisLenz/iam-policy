{
    "policy": [
        {
            "Action": [
                "ec2:SendDiagnosticInterrupt"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:instance/{instance_id}"
            ]
        }
    ]
}