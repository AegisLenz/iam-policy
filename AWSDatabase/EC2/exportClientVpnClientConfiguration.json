{
    "policy": [
        {
            "Action": [
                "ec2:ExportClientVpnClientConfiguration"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:client_vpn_endpoint/{client_vpn_endpoint_id}"
            ]
        }
    ]
}