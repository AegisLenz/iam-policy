{
    "policy": [
        {
            "Action": [
                "ec2:EnableVolumeIO"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:volume/{volume_id}"
            ]
        }
    ]
}