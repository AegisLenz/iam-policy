{
    "policy": [
        {
            "Action": [
                "ec2:DisassociateTransitGatewayRouteTable"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:attachment/{attachment_id}",
                "arn:aws:ec2:{region}:{account_id}:transit-gateway-route-table/{transit_gateway_route_table_id}"
            ]
        }
    ]
}