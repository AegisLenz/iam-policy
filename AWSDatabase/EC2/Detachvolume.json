{
    "policy": [
        {
            "Action": [
                "ec2:DetachVolume"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:instance/{instance_id}",
                "arn:aws:ec2:{region}:{account_id}:volume/{volume_id}"
            ]
        }
    ]
}