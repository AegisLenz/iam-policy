{
    "policy": [
        {
            "Action": [
                "ec2:ConfirmProductInstance"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:instance/{instance_id}"
            ]
        }
    ]
}
