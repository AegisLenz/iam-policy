{
    "policy": [
        {
            "Action": [
                "ec2:DeleteNetworkAclEntry"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:network-acl/{network_acl_id}"
            ]
        }
    ]
}