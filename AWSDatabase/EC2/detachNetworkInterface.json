{
    "policy": [
        {
            "Action": [
                "ec2:DetachNetworkInterface"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:instance/{instance_id}",
                "arn:aws:ec2:{region}:{account_id}:network_interface/{attachment_id}"
            ]
        }
    ]
}