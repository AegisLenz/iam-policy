{
    "policy": [
        {
            "Action": [
                "ec2:CreateCarrierGateway"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:carrier-gateway/*",
                "arn:aws:ec2:{region}:{account_id}:vpc/{vpc_id}"
            ]
        }
    ]
}