{
    "policy": [
        {
            "Action": [
                "ec2:ModifyTrafficMirrorFilterRule"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:traffic-mirror-filter-rule/{traffic_mirror_filter_rule_id}",
                "arn:aws:ec2:{region}:{account_id}:traffic-mirror-filter/{traffic_mirror_filter_id}"
            ]
        }
    ]
}