{
    "policy": [
        {
            "Action": [
                "ec2:GetTransitGatewayAttachmentPropagations"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:attachment/{attachment_id}"
            ]
        }
    ]
}