{
    "policy": [
        {
            "Action": [
                "ec2:ModifyAddressAttribute"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:elastic-ip/{allocation_id}"
            ]
        }
    ]
}