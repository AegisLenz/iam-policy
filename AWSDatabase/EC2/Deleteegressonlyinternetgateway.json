{
    "policy": [
        {
            "Action": [
                "ec2:DeleteEgressOnlyInternetGateway"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:egress-only-internet_gateway/{internet_gateway_id}"
            ]
        }
    ]
}