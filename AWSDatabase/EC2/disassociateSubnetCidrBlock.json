{
    "policy": [
        {
            "Action": [
                "ec2:DisassociateSubnetCidrBlock"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:subnet/{subnet_id}"
            ]
        }
    ]
}