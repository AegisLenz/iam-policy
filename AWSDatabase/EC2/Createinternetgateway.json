{
    "policy": [
        {
            "Action": [
                "ec2:CreateInternetGateway",
                "ec2:CreateTags"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:internet_gateway/{internet_gateway_id}"
            ]
        }
    ]
}