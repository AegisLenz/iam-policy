{
    "policy": [
        {
            "Action": [
                "ec2:ModifyVpnTunnelOptions"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:vpn-connection/{vpn_connection_id}"
            ]
        }
    ]
}