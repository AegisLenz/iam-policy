{
    "policy": [
        {
            "Action": [
                "ec2:CreateVpnGateway"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:vpn_gateway/{vpn_gateway_id}"
            ]
        }
    ]
}