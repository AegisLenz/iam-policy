{
    "policy": [
        {
            "Action": [
                "ec2:ReleaseHosts"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:dedicated-host/{dedicated_host_id}"
            ]
        }
    ]
}