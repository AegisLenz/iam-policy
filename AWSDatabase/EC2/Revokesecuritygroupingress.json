{
    "policy": [
        {
            "Action": [
                "ec2:RevokeSecurityGroupIngress"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:security-group/*"
            ]
        }
    ]
}