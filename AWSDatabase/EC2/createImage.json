{
    "policy": [
        {
            "Action": [
                "ec2:CreateImage"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}::image/*",
                "arn:aws:ec2:{region}:{account_id}:instance/{instance_id}"
            ]
        }
    ]
}