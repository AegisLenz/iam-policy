{
    "policy": [
        {
            "Action": [
                "ec2:SearchLocalGatewayRoutes"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:local-gateway-route-table/{local_gateway_route_table_id}"
            ]
        }
    ]
}