{
    "policy": [
        {
            "Action": [
                "ec2:GetManagedPrefixListEntries"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:prefix-list/{prefix_list_id}"
            ]
        }
    ]
}