{
    "policy": [
        {
            "Action": [
                "ec2:ModifyTrafficMirrorSession"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:traffic-mirror-session/{traffic_mirror_session_id}",
                "arn:aws:ec2:{region}:{account_id}:traffic-mirror-target/{traffic_mirror_target_id}",
                "arn:aws:ec2:{region}:{account_id}:traffic-mirror-filter/{traffic_mirror_filtert_id}"
            ]
        }
    ]
}