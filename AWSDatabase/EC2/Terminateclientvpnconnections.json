{
    "policy": [
        {
            "Action": [
                "ec2:TerminateClientVpnConnections"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:client-vpn-endpoint/{client_vpn_endpoint_id}"
            ]
        }
    ]
}