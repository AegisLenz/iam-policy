{
    "policy": [
        {
            "Action": [
                "ec2:ModifySubnetAttribute"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:subnet/{subnet_id}"
            ]
        }
    ]
}