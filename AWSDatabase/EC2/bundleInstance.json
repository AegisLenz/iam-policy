{
    "policy": [
        {
            "Action": [
                "ec2:BundleInstance"
            ],
            "Effect": "Allow",
            "Resource": [
                "arn:aws:ec2:{region}:{account_id}:instance/{instance_id}",
                "arn:aws:s3:::{bucket_name}/*"
            ]
        }
    ]
}
